
Pass=true
ntests=0
times=1
[[ ! -z $1 ]] && times=$1
for (( j=0;j<$times;j++ )); do
	./machine 2>/dev/null
	result=$?
	if [[ $result != 0 ]]; then Pass=false; fi
	((ntests++))
done
echo "Tested ${ntests} times, Pass:${Pass}"

